<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/lv_processmanager_item_selector"
    android:orientation="horizontal" >

    <ImageView
        android:id="@+id/iv_processmanager_item_icon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center_vertical"
        android:padding="8dp" />

    <LinearLayout
        android:id="@+id/line_processmanager_item_msg"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/tv_processmanager_item_name"
            style="@style/matchAndWrap"
            android:ellipsize="start"
            android:singleLine="true"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/tv_processmanager_item_mem"
            style="@style/matchAndWrap"
            android:layout_marginTop="3dp"
            android:ellipsize="start"
            android:singleLine="true" />
    </LinearLayout>

    <CheckBox
        android:id="@+id/cb_processmanager_item_check"
        style="@style/wrapAndWrap"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="5dp"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false" />

</LinearLayout>